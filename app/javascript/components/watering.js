const water = document.querySelector('#watering');
water.addEventListener('onclick', (event) => {
  event.preventDefault();
  list.innerHTML = '';
  const input = document.querySelector('#search-input');
});
