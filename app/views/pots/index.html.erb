<ul>
  <% @pots.each do |pot| %>
    <%= link_to "#{pot.plant.name}", pot_path(pot) %>
    <ol>
  <%=  pot.plant.name %>
  <%=  pot.plant.care %>
    </ol>
  <% end %>
</ul>

<%= form_tag pots_path, method: :get do %>
  <%= text_field_tag :query, params[:query], class: "form-control" %>
<% end %>
