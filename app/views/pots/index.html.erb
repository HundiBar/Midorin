<div class="container mx my-5">

<% @pots.each do |pot| %>
      <%= link_to "#{pot.plant.name}", pot_path(pot) %>
      <ol>
    <%=  pot.plant.name %>
    <%=  pot.plant.care %>
      </ol>
    <% end %>



</div>
