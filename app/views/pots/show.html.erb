<div class="container">
    <h1>It's me, NICKNAME </h1>
    <h3>I'm DATE old</h3>
        <div class="info section">
            <p class="general info">
                <%= image_tag @plant.cover_picture %>
                Name: <%= @plant.name %>
                Scientic name: <%= @plant.scientific_name %>
                Description: <%= @plant.description %>
                Care: <%= @plant.care %>
                Temperature: <%= @plant.temperature %>
                Light : <%= @plant.light %>
                Toxicity: <%= @plant.toxicity %>
                Water: LAST WATER
            </p>
        </div>
        <div class="d-flex justify-content-center align-items-center mt-18 mb-12">
            <%= link_to "Back to garden", pots_path, class:'btn btn-primary'%>
        </div>
</div>